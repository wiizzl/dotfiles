---
- name: Install shell
  become: true
  ansible.builtin.dnf:
    name: fish
    state: present

- name: Change default user shell
  become: true
  ansible.builtin.user:
    name: "{{ ansible_facts['user_id'] }}"
    shell: /usr/bin/fish
